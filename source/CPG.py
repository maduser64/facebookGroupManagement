class CPG:

    def __init__(self):

        def grabConfigURL(self, urlType):
            self.urlType = urlType

            """
            goes into config file and reads the parameter requested
puts it into a string variable
returns the string variable. In this case the URL
            """