from genericImportClass import *

class MNS:

    def __init__(self, scrapeUrl):
        self.scrapeUrl = scrapeUrl

    def grabSoup(self self.scrapeUrl):

    """
    grabs soup URL from config file stores in variable
while seemore link is present
	click the see more link

make soup

grab all html div tags with class fsl and store in array
for I in placeholder grab all a tags.text and store in another array

return the array with the text.
    """